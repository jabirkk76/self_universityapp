import 'package:flutter/material.dart';

class SavedTwoScreen extends StatelessWidget {
  const SavedTwoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Saved Two'),
      ),
    );
  }
}
