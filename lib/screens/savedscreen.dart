import 'package:flutter/material.dart';

class SvedOneScreen extends StatelessWidget {
  const SvedOneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Saved one'),
      ),
    );
  }
}
